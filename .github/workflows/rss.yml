name: RSS Ingest R107

on:
  schedule:
    - cron: "0 */12 * * *"   # co 12 godzin (o 00:00 i 12:00)
  workflow_dispatch:        # ręczne uruchomienie

jobs:
  ingest:
    runs-on: ubuntu-latest
    steps:
      - name: Call Supabase RSS ingest
        run: |
          curl -X POST https://pupwrokffpezkywkyogl.supabase.co/functions/v1/ingest-rss \
            -H "Content-Type: application/json" \
            -H "x-cron-secret: ${{ secrets.CRON_SECRET }}"